# URL of your deployed Cloudflare Worker (no trailing slash)
# In development with `vite`, leave this unset — requests to /api/* are proxied to localhost:8787
VITE_API_URL=https://matchcraft-api.your-name.workers.dev
